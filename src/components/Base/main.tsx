import ControlPanel from "../filters/controlPanel";
import Grid from "../grid/grid";

export default function Main() {
  return (
    <section>
      <ControlPanel />
      <Grid />
    </section>
  );
}
